export const subscriptionStyle = (theme) => ({
    container: {
      display: 'flex',
      flexDirection: 'column'
    },
    userField: {
      marginBottom: `${theme.typography.pxToRem(15)} !important`
    }
  });
  